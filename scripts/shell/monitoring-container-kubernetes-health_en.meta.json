{
  "name": "View Monitoring Container Kubernetes Health",
  "url": "https://raw.githubusercontent.com/Mio888888/OpsBatch-Library/main/scripts/shell/monitoring-container-kubernetes-health_en.sh",
  "language": "shell",
  "category": "monitoring",
  "tags": [
    "container",
    "docker",
    "kubernetes",
    "monitoring",
    "health-check",
    "read-only"
  ],
  "risk": "medium",
  "description": "Runs the view monitoring container kubernetes health script for monitoring operations. Review the output because it may be noisy or expose operational details.",
  "parameters": [
    {
      "name": "mode",
      "description": "Mode value.",
      "required": false,
      "default": "auto"
    },
    {
      "name": "namespace",
      "description": "Namespace value.",
      "required": false,
      "default": "default"
    },
    {
      "name": "name_filter",
      "description": "Name filter value.",
      "required": false,
      "default": ""
    },
    {
      "name": "max_restarts_warn",
      "description": "Max restarts warn value.",
      "required": false,
      "default": "3"
    },
    {
      "name": "max_restarts_crit",
      "description": "Max restarts crit value.",
      "required": false,
      "default": "10"
    }
  ],
  "platform": [
    "linux",
    "macos"
  ]
}
