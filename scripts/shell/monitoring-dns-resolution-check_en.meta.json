{
  "name": "Monitoring DNS Resolution Check",
  "url": "https://raw.githubusercontent.com/Mio888888/OpsBatch-Library/main/scripts/shell/monitoring-dns-resolution-check_en.sh",
  "language": "shell",
  "category": "monitoring",
  "tags": [
    "dns",
    "network",
    "monitoring",
    "threshold",
    "read-only"
  ],
  "risk": "medium",
  "description": "Runs the monitoring dns resolution check script for monitoring operations. Review the output because it may be noisy or expose operational details.",
  "parameters": [
    {
      "name": "target_host",
      "description": "Target host or address.",
      "required": false,
      "default": "example.com"
    },
    {
      "name": "dns_server",
      "description": "Dns server value.",
      "required": false,
      "default": ""
    },
    {
      "name": "timeout_seconds",
      "description": "Timeout seconds value.",
      "required": false,
      "default": "5"
    },
    {
      "name": "latency_warn_ms",
      "description": "Latency warn ms value.",
      "required": false,
      "default": "500"
    },
    {
      "name": "latency_crit_ms",
      "description": "Latency crit ms value.",
      "required": false,
      "default": "1000"
    }
  ],
  "platform": [
    "linux",
    "macos"
  ]
}
