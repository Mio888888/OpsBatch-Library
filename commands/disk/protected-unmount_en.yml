name: Protected Unmount
url: https://raw.githubusercontent.com/Mio888888/OpsBatch-Library/main/commands/disk/protected-unmount_en.sh
category: disk
tags:
- remediation
- disk
- mount
- unmount
risk: high
description: Runs the protected protected unmount workflow for disk, filesystem, and
  storage inspection. It requires explicit intent or confirmation before making state-changing
  changes.
platform:
- linux
- macos
parameters:
- name: TARGET_MOUNT
  description: Target mount value.
  required: true
  default: null
- name: CONFIRM_UMOUNT
  description: Confirm umount value.
  required: false
  default: null
